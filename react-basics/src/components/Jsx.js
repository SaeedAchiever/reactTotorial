import React from 'react'

function Jsx() {
  return (
    <div>
        {
            /*
                JSX(Javascript XML) are HTML like tag that
                is used to design the UI of React Applications
                e.g <p> </p>, <div> </div>, <a> <a/> etc
            */
        }
        <h3>Hello, from JSX</h3>
    </div>
  )
}

export default Jsx